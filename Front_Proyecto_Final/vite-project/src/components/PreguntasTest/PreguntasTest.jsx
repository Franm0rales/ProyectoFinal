import { useAuthContext } from "../../context/AuthContext/AuthContext";

export default function PreguntasTest({ key, test }) {
  const { puntuacionTest, setPuntuacionTest } = useAuthContext();
  return <></>;
}
